const Buscar = (procurado) => {
  let fila = []
  while (fila[0] !== null) {
    if (fila[0] == procurado) {
      return true;
    } else {
      foreach(filho in fila) {
        
      }
    }
  }
};
